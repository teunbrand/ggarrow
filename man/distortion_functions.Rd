% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distortions.R
\name{distortion_functions}
\alias{distortion_functions}
\alias{distort_sinewave}
\alias{distort_sawtooth}
\alias{distort_squarewave}
\title{Line distortion functions}
\usage{
distort_sinewave(length = 4, width = 2, n = 30L)

distort_sawtooth(length = 4, width = 2)

distort_squarewave(length = 4, width = 2)
}
\arguments{
\item{length}{A positive \code{numeric(1)} setting the length along the path in
millimetres that one repetition of the distortion occupies. Actual length
may end up slightly wider to cover a remainder length.}

\item{width}{A \code{numeric(1)} setting the width (amplitude) of the distortion
in millimetres. Negative values can be used to flip the distortion.}

\item{n}{An \code{integer(1)} setting the number of vertices to use for the path.}
}
\value{
A \verb{<matrix[n, 2]>} with \code{x} and \code{y} columns describing a path.
It may have a \code{size} attribute.
}
\description{
These are functions that build a pattern that can be used as the \code{distort}
argument in arrow geoms. They produce one 'oscillation' that will be repeated
to distort a line.
}
\details{
It is possible to write your own \verb{distort_*()} function. The assumption
is that an oscillation starts at the \verb{(0, 0)} coordinate and ends at the
\verb{(length, 0)} coordinate.

The exact length of an oscillation will be fitted based on the arc-length of
the path it will be placed on. If the arc-length will not accommodate an
exact integer number of oscillations, the oscillations will be stretched to
cover the whole arc-length.
}
\section{Functions}{
\itemize{
\item \code{distort_sinewave()}: Follows a sine waveform.

\item \code{distort_sawtooth()}: Follows a triangular waveform.

\item \code{distort_squarewave()}: Follows a square waveform.

}}
\examples{
plot(distort_sinewave(), type = 'l')
}
